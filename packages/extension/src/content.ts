/**
 * Content script. Currently a no-op placeholder; blocking is done by declarativeNetRequest
 * redirecting main_frame loads. Future use: injecting a soft overlay banner during a session.
 */
export {};
